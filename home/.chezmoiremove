# Remove deployed copies of files no longer managed (pyenv/poetry cleanup).
# These live in non-exact_ dirs, so deleting the source does not remove them.
.config/fish/completions/pyenv.fish
.config/fish/conf.d/60_poetry.fish
.claude-mem
